const pageTemplate = teamData => {

    let cards = '';

    let finalHTML = `<html><body>${cards}</body></html>`
}

// May have to make a function that calls general page HTML, then a new function that calls each card in every time that an employee is created

module.exports = pageTemplate